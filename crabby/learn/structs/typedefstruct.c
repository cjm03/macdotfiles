#include <stdio.h>
#include <stdbool.h>

// Typedef is used to define a synonym name for a predefined type. For example:
typedef unsigned long ulong;
// Now if we wanted to declare a variable of type unsigned long, we can simply use 'ulong' 
// instead of typing out 'unsigned long'

//

